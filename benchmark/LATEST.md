# Свежие результаты прогонов

Дайджест автоматических прогонов (дневной 11:37, ночной 02:47). Новые записи сверху.
Полные отчёты — в Notion, спринт-страницы под «⚡ Wiki-LLM: скорость локального инференса».
Подробные замеры — в `baseline-*.md` / `after-*.md` рядом.

---

## 2026-09-01 23:15 – 2026-09-02 01:45 (интерактивно, не по расписанию)
**Сделано:** сняты первые полные базовые числа по фазам на всех 32 вопросах —
то, что три прогона подряд стояло в «Дальше». Три прогона на одном телефоне
(S23), одной модели (QVikhr-3-4B-Instruction-Q4_0, NPU, MTP вкл.) и одном
наборе: build-40 «до», build-42 «после» правок промпта и истории, build-42 с
выключенным «Думать». Логи и разборы рядом: `baseline-turn-hexagon40.*`,
`after-turn-hexagon42.*`, `after-turn-hexagon42-nothink.*`.

**Метрики (медиана модельного хода, 13 измеренных ходов из 17 в каждом прогоне):**

| | build-40 | build-42 | build-42, noThink |
|---|---|---|---|
| всего | 153 140 мс | 72 309 мс | **34 283 мс** |
| поиск + чтение | 2 121 мс (1.4%) | 2 216 мс (3.0%) | 2 280 мс (6.6%) |
| prefill | 20 890 мс (13.6%) | 7 064 мс (9.8%) | 8 347 мс (24.3%) |
| decode | 132 510 мс (86.5%) | 64 162 мс (88.7%) | 23 506 мс (68.6%) |
| промпт-токенов | 5 608 | 2 409 | 2 676 |
| сген. токенов | 501 | 367 | 128 |
| decode tok/s | 3.8 | 5.7 | 5.4 |
| быстрый путь | 422 мс | 413 мс | 610 мс |

**4.5× по медиане хода** — и ни одного изменения в ядре: убрана инструкция
«собери ВСЕ факты», протёкшая из списочной ветки во все обычные вопросы;
история чата перестала переигрываться целиком (было «Generating: 61 msgs» к
30-му вопросу); выключен блок «размышлений». Декод ускорился сам, 3.8 → 5.7
tok/s: короче промпт → меньше KV-кэш → дешевле внимание на токен.

Доля фаз перевернулась: декод 86.5% → 68.6%, prefill 13.6% → 24.3%,
извлечение 1.4% → 6.6%. Дальнейшая работа только над декодом уже неверна.

**Найдено (важнее скорости):**
1. **Кореференция портила самодостаточные вопросы.** Правило было «короче 25
   символов ИЛИ есть местоимение». «что производит АвтоВАЗ» — 22 символа —
   получил в запрос хвост предыдущего вопроса и ответил «…Большое кра…».
   Половина набора короче 25 символов. Починено в `cda326b`: занимаем тему,
   только если есть местоимение или своей сущности нет вовсе.
2. **Модель периодически выдаёт мусор.** ~2 из 17 модельных ходов в обоих
   прогонах: `urple ту rebut看似sume fadebytes初次getMockierte Stamford…`,
   1600 токенов и 330 с в худшем случае. НЕ дрейф от штрафа за повтор —
   повторов нет, токены случайны по языкам, то есть распределение почти
   равномерное. Prefill/decode tok/s при этом нормальные, в логе ни ошибки.
   **Не воспроизводится** в свежем чате: тот же вопрос 4 раза подряд — 4
   чистых ответа. Нужно состояние длинного прогона.
3. **Раннер при таймауте подписывал ответы не теми вопросами** (`62e3ab7`).
4. Подтверждён дефект №3 из записи ниже: после переустановки RAG выключен.

**Коммиты:** 70b6c81, 2cbb286, 62e3ab7, cda326b. CI зелёный (run 33555130850,
hexagon-42). Прогоны 40/42/42-noThink сняты на устройстве, правка cda326b —
только собрана, на телефоне не мерена.
**Дальше:** воспроизвести мусор (гонять набор с MTP выкл. и сравнить долю
испорченных ходов — это разделит гипотезы «NPU» и «откат KV в MTP»); затем
перемерить набор на build с cda326b. Prefill теперь 24% хода — резать
контекст стало осмысленно, но только после мусора.

## 2026-09-01 23:07–23:40 (ночь)
**Сделано:** закрыт дефект №1 из записи ниже — тот, который там помечен как
блокер разбивки prefill/decode. Нативная сторона считает тайминги ДО
`onComplete` и всегда их печатает; терять их умела только Kotlin-сторона:
`LlmEvent.Done` уходит терминальным `trySend` по каналу, а у остановленного
хода коллектор уже отменён и канал закрыт, поэтому событие молча пропадает и
`[TURN] end` печатает нули. Тайминги теперь идут отдельным колбэком `onStats`
прямо из `onComplete`, мимо канала. `score_turn.py` вдобавок не скорит ход без
prefill и decode, а считает его отдельно.
**Метрики:** первая живая разбивка модельной части хода (S23, hexagon-40, один
RAG-ход): **prefill 2904 tok / 7290 мс (398 tok/s), decode 221 tok / 37739 мс
(5.9 tok/s)** — из 46 285 мс хода decode **81.5%**, prefill 15.7%. Это первое
число по фазам с моделью, а не оценка по руке; n=1, ход был прерван чужим
прогоном, поэтому «всего» не равно естественной длине хода. На синтетическом
логе один нескоренный ход рядом с одним нормальным ронял медиану decode вдвое
(37700 → 18850 мс) — ровно та фаза, ради которой скорер и писался.
**Не проверено на устройстве:** телефон всё время держал параллельный прогон
(его широковещательные вопросы и останавливали мою генерацию — отсюда и нули),
APK с правкой не ставился. CI зелёный: run 33554678002, hexagon-app, success.
Базовые числа по всем 32 вопросам по-прежнему не сняты.
**Коммиты:** 6fbbd48.
**Дальше:** поставить hexagon-41 и повторить один ход — `[TURN] end` обязан
показать ненулевые prefill/decode; затем `run_turn.sh` целиком, когда телефон
свободен и никто не шлёт вопросы параллельно.

## 2026-09-01 23:05–23:35 (день)
**Сделано:** телефон впервые за четыре прогона на месте — снят первый замер по
фазам НАСТОЯЩЕГО хода (hexagon-40, S23, `run_turn.sh`). Дошло 15 вопросов из 32:
раннер обогнал сам себя на первом медленном ходе и умер. Прогон вскрыл три
дефекта, каждый из которых важнее исходной задачи.
**Метрики:** быстрый путь **14/15 ходов, медиана 409 мс** — первые живые числа
по полному ходу, а не по одной retrieval-половине. Единственный модельный ход:
всего 46 285 мс, из них поиск 996 мс (2.2%), чтение статей 225 мс (0.5%) →
**на извлечение уходит 2.7% хода, 97.4% забирает модель.** Разбить эти 97.4% на
prefill и decode ПО-ПРЕЖНЕМУ НЕ УДАЛОСЬ: `ptok/prefill/decode` пишутся нулями.
**Найдено:** (1) `LlmEvent.Done` не доходит до ChatViewModel — `stats` остаётся
null, `[TURN] end` печатает нули, и вчерашняя разметка меряет не то, ради чего
делалась; (2) вопросы бенчмарка идут в ОДНУ беседу — к 15-му ходу «Generating:
32 msgs», промпт растёт от вопроса к вопросу и ходы неindependentные;
(3) тумблер RAG и выбор ZIM не переживают перезапуск (`_ragEnabled = false`,
не в prefs, в отличие от `KEY_LAST_MODEL`) — после переустановки приложение
молча отвечает без вики и галлюцинирует.
**Коммиты:** см. ниже.
**Дальше:** починить `LlmEvent.Done` — без него разбивка prefill/decode
недостижима; сбрасывать беседу на каждый вопрос бенчмарка; сохранять выбор RAG.

## 2026-08-31 11:38–12:00 (день)
**Сделано:** телефон снова не подключён, поэтому взят инструментальный долг, а не
правка качества. `run_probe.sh` останавливается перед моделью, и всё, что он
видит, — первая секунда хода; утверждение «декод ≈ 90% времени RAG-ответа»
держалось на одиночных ручных замерах и по набору не снималось ни разу. Ход
теперь размечен: `[TURN] begin/end` в `ChatViewModel`, `[PHASE] search/read/ctx`
в `RagPromptBuilder` (индекс и чтение статей биллятся раздельно — их двигают
разные рычаги), плюс стоимость ПРОМАХА быстрого пути, за которую платит каждый
медленный ход и которую не считали нигде. Добавлены `run_turn.sh` (32 вопроса
настоящими ходами чата) и `score_turn.py` (медиана мс и доля хода по фазам,
tok/s декода и prefill). Поведение не менялось — только измерение.
**Метрики:** на устройстве **не измерено**, `adb devices` пуст третий прогон
подряд. Офлайн проверен скорер на синтетическом логе: 7 ходов разобрано,
прерванный ход без строки `end` отброшен, а не слит с соседним. CI зелёный
(run 33374295715, hexagon-app, success).
**Коммиты:** cdde976.
**Дальше:** прогнать `run_turn.sh` на S23 — это первые базовые числа по фазам
полного ответа; затем резать контекст до предложений (`relevantChunk` сейчас
отдаёт сырое окно символов) и мерить тем же скорером по строке `ctx`.

## 2026-08-31 02:47–03:20 (ночь)
**Сделано:** телефона не было — правка без устройства. Скорер читал только
`expect_article`, поэтому быстрый ответ из ВЕРНОЙ статьи, но не из того поля,
считался чистым попаданием; `expect_field` лежал в эталоне с самого начала и не
читался никогда. Добавлен `wrong-field-rate` — ровно тот риск, который открыло
расширение `matchByLabel` на любую метку карточки (9ad22fb).
**Метрики:** пересчёт трёх сохранённых прогонов (29–30-го): `wrong-field-rate`
**0/14 = 0%** (регрессий не было, но до сегодня за этим не следило ничто);
recall@1 91%, false-fast 0%, 14/14 infobox — без изменений. Найдено: mh02
отвечается быстро из «Место рождения» при `expect_field: null` — судить не по
чему, теперь отдельная строка «fast answers with no reference field: 1».
**Не измерено:** ничего на устройстве, `adb devices` пуст. Kotlin не тронут.
**Коммиты:** 2bd687a.
**Дальше:** прогнать `run_probe.sh` на S23 — новой метрике нужны живые числа;
решить эталонное поле для mh02 (вопрос многошаговый, строки карточки под него
нет). Для обрезки контекста инструмента пока нет: размер промпта не логируется
нигде (только `prompt.take(500)`), ZIM на маке отсутствует — без лога токенов
«2434 → ~900» не с чем сравнивать даже с телефоном в руках.

## 2026-08-30 02:47–03:05 (ночь)
**Сделано:** ничью двух догадок решает объём статьи. «из чего состоит атмосфера
Марса» держал на ранге 1 мальтийский город «Марса»: обе формы одной n-граммы,
обе точные, обе identity, обе 1080 — написанная форма шла первой только потому,
что `phraseForms` выдаёт её раньше угаданного именительного. Тай-брейк смотрит
только на верхнюю группу очков (ниже она ранг 1 не меняет) и только при явном
перевесе (2×); победитель переоценивается на +5, а не переставляется, потому что
`RagPromptBuilder` сортирует пиннутые хиты по очкам заново.
**Метрики:** recall@1 28/32 88% → **29/32 91%**, MRR 0.922 → **0.938**,
pr02 ранг **2 → 1**. Регрессий нет: recall@3 97% → 97%, быстрый путь 14/14 →
14/14, false-fast 0% → 0%, ни один другой вопрос ранг не сдвинул; медиана поиска
821 → 813 мс, у самого pr02 811 → 768 мс. Тай-брейк сработал ровно один раз за
32 вопроса (`Марс=658430` против `Марса=7479`, 88×) — две декомпрессии.
Замер на S23, билд hexagon-39, `run_probe.sh 20`.
**Коммиты:** 7abb966.
**Дальше:** на ранге 2 остались только ls01 и ls02, где спорен сам эталон
(см. запись за 28-е); pr03 — недостижимый эталон (редирект «Пенициллин» →
«Бензилпенициллин»). Чисто поисковых промахов на наборе больше нет — следующий
рычаг лежит в декоде (91% времени RAG-ответа) и в невзятых базовых числах по
полному ответу: точность и латентность по фазам с моделью ни разу не сняты.

## 2026-08-29 11:38–12:00 (день)
**Сделано:** заголовок, вернувшийся из индекса без изменений, — более сильная
заявка на статью, чем тот, до которого дошли через редирект. Все формы одной
n-граммы получают один балл, поэтому «Океаны»→«Мировой океан» и
«Океана»→«(224) Океана» стояли выше «Океана»→«Океан». `probe()` даёт +10, если
заголовок хита равен догадке; 10 меньше шага n-граммы (20) намеренно — это
тай-брейк внутри одной n-граммы и ничто больше (при 25+ город «Гагарин» обошёл
бы «Гагарин, Юрий Алексеевич», до которого пробер как раз идёт редиректом).
**Метрики:** recall@1 27/32 84% → **28/32 88%**, MRR 0.901 → **0.922**,
ls03 ранг **3 → 1** («Океан» s=1030 против «Мировой океан» s=1020). Регрессий
нет: recall@3 97% → 97%, быстрый путь 14/14 → 14/14, false-fast 0% → 0%,
ни один другой вопрос ранг не сдвинул; медиана поиска 817 → 769 мс (шум).
Замер на S23, билд hexagon-38, `run_probe.sh 20`.
**Коммиты:** 30b1c48.
**Дальше:** на ранге 2 остались pr02, ls01, ls02. pr02 — ничья 1070 между
статьёй «Марса» (мальтийский город, точное совпадение) и «Марс»: обе формы
одной n-граммы и обе точные, тай-брейк не помогает. У ls01/ls02 спорен сам
эталон (см. запись за 28-е). pr03 «кто открыл пенициллин» — вне топ-20 четвёртый
прогон подряд: в снапшоте «Пенициллин» ведёт на «Бензилпенициллин», то есть
эталон недостижим — это дефект эталона, а не поиска.

## 2026-08-29 02:48–03:15 (ночь)
**Сделано:** вопрос, который сам называет уточнение, больше не приземляется на
страницу значений. «кто режиссёр фильма Сталкер» пиннил «Сталкер» (страница
значений, ни карточки, ни тела) на 1070, а «Сталкер (фильм)» лежал рангом ниже
на BM25 s=99 — в том же списке кандидатов. Быстрый путь этим не страдал
(`qualifierGap` брал фильм и отвечал за 108 мс), у выдачи такого правила не было.
`qualifierGap` переехал в `EntityTitleProbe`, поисковая ветка переоценивает
кандидата вида «<пиннутый заголовок> (уточнение)» на +15 над своей базой.
Новых обращений к индексу нет — сиблинг и так был кандидатом.
**Метрики:** recall@1 26/32 81% → **27/32 84%**, MRR 0.885 → **0.901**,
ib14 ранг **2 → 1** («Сталкер (фильм)» s=1085 против «Сталкер» s=1070).
Регрессий нет: recall@3 97% → 97%, быстрый путь 14/14 → 14/14, false-fast
0% → 0%; ни один другой вопрос не сдвинул ранг. Латентность двух прогонов
несопоставима (baseline снят с приложением в фоне, медиана 2626 мс; after —
766 мс, в диапазоне дневных 838 мс), правка обращений не добавляет.
Замер на S23, билд hexagon-37, `run_probe.sh 20`.
**Коммиты:** f81093b.
**Дальше:** на ранге 2–3 остались pr02, ls01, ls02, ls03; у ls01/ls02 спорен
сам эталон (см. запись за 28-е). ls03 — по-прежнему ничья 1020 между формами
«океаны»/«океана»/«океан»: `phraseForms` даёт всем формам один балл, стоит
убывающий штраф по индексу догадки. pr03 «кто открыл пенициллин» — единственная
статья вне топ-20, третий прогон подряд.

## 2026-08-28 11:37–12:05 (день)
**Сделано:** список-вопрос сам называет свою статью-список. «перечисли спутники
Юпитера» держал «Спутники Юпитера» на 890 — ниже голого «Юпитер» (1070): это
ATTRIBUTE_PENALTY, правило «Мэр Москвы не должен обыгрывать Москву». Для фактоида
оно верно, для перечисления статья-атрибут и есть цель. Вместо инверсии штрафа
tier 0 `listAwareTitleProbes` получил фразу из самого вопроса (обрезали оболочку
команды — остаток часто и есть заголовок; в tier 0 до сих пор были только
лидерские шаблоны «Градоначальники X»). Фраза отбрасывается, если внутри остался
стоп-токен, поэтому «Океаны на Земле» не пробуется.
**Метрики:** recall@1 78% (25/32) → **81% (26/32)**, MRR 0.865 → **0.885**;
ls05 ранг **3 → 1** («Спутники Юпитера» s=2000, bodyLen=22957, теперь seed
list-извлечения); recall@3 97% → 97%, быстрый путь 14/14 → 14/14, false-fast
0% → 0%; медиана поиска 824 → 838 мс (шум). Больше ни один вопрос не сдвинулся:
ls01 по-прежнему берёт «Градоначальники Тольятти», ls04 остался ранг 1
(«Произведения Достоевского» — не самостоятельная статья). Замер на S23,
билд hexagon-36, `run_probe.sh 20`.
**Коммиты:** f2c86b7.
**Дальше:** осталось на ранге 2–3: ib14, pr02, ls01, ls02, ls03. У ls01/ls02
эталон спорен — «Градоначальники Тольятти» и «Планеты Солнечной системы» отвечают
на вопрос лучше, чем городская/системная статья; стоит пересмотреть expect_article,
а не скорер. ls03 — трёхсторонняя ничья 1020 между «Мировой океан», «(224) Океана»
и «Океан»: пины от нечёткого тира `lookupTitleExact` получают тот же балл, что и
точное совпадение заголовка.

## 2026-08-28 02:47–03:10 (ночь)
**Сделано:** три причины, по которым поле, которое в карточке ЕСТЬ, не доходило до
`matchByLabel`: дедуп строк по значению выкидывал разные метки с одинаковым значением
(теперь дедуп только для `lines`, которые идут в промпт; `fields` полные);
`QUESTION_STEMS` был написан в 4 символа, а `stems()` отдаёт 6, так что «какой»,
«когда», «сколько» вообще не фильтровались; и добавлен словарь вопрос→метка на
7 пар («где умер» → «Место смерти», «гражданство» → «Страна»). Промах теперь логирует,
во что свёлся вопрос и что предложила карточка.
**Метрики:** регрессий нет — recall@1 78% → **78%**, MRR 0.865 → **0.865**, быстрый путь
14/14 infobox → **14/14**, false-fast **0% → 0%**, медиана поиска 801 → 824 мс (шум).
Из трёх целевых вопросов исправлены два: «где умер Лев Толстой» → «Место смерти:
станция Астапово» (110 мс), «какое гражданство у Людвига Баумана» → «Страна: Австрия»
(317 мс); оба раньше уходили в RAG. Третий, «какой рост у Юрия Гагарина», — не баг:
в карточке снапшота поля «Рост» нет вовсе (лог показал все 11 меток).
**Коммиты:** 9db476b, b4d7ef4. Билды hexagon-34/35, замеры на S23.
**Дальше:** покрытие быстрого пути по 32 вопросам не сдвинулось (15/32) — целевые
вопросы в набор не входят; стоит добавить их туда, иначе прогресс по matchByLabel
не виден ни одной метрике.

## 2026-08-27 11:37–12:15 (день)
**Сделано:** recall@1 сдвинулся с мёртвой точки. Причина была не в скоринге, а в
дедупликации: `distinctBy { it.path }` оставлял ПЕРВУЮ копию пути, а порядок
конкатенации перестал совпадать с убыванием score — walker пиннит свой seed на
1000, тогда как EntityTitleProbe даёт той же статье 1090. Поэтому «Гагарин, Юрий
Алексеевич» доходил до сортировки с 1000 и проигрывал голому «Юрий» (1070).
Теперь дедуп берёт максимум; следом вернул гейт title-boost из 829b7ff (он и не
дал эффекта в прошлый раз именно потому, что порядок ломался ЭТАПОМ РАНЬШЕ); и
seed walker'а больше не входит по score нечёткого тира (`lookupExactTitle` не
точен — «Администрация Екатеринбурга» занимала ранг 1 вместо «Екатеринбург»).
**Метрики:** recall@1 **52% (16/31) → 78% (25/32)**, MRR 0.720 → **0.865**;
recall@3 97% → 97%; быстрый путь 100% infobox, false-fast 0% → 0%; вопросов
отскорено 31/32 → **32/32**. На 1 ранг поднялись ib04, ib10, ib11, ib12, pr05,
pr06, mh02, mh03, mh05 — вниз не ушёл никто. Латентность поиска сравнивать
нельзя: раннер перестал спать 3 с между вопросами (см. ниже), 815 → 2422 мс при
идентичных cand= по каждому вопросу, то есть лишней работы поиск не делает.
**Коммиты:** fcef63a, 5b4950e, + правка ожидания в раннере.
**Дальше:** проверить латентность старым темпом (пауза между вопросами) — рост
почти наверняка троттлинг/прогрев, но это догадка. Осталось на ранге 2–3:
ib14, pr02, ls01, ls02, ls03, ls05.

## 2026-08-27 02:47–03:10 (ночь)
**Сделано:** быстрый путь научился видеть скобочных братьев по заголовку — спрашиваем
и индекс подсказок (тот, по которому дополняет строку поиска Kiwix), и окно братьев
расширено 8 → 32, потому что порядок в нём алфавитный: «Сталкер (фильм)» стоял за
«Сталкер (программа)».
**Метрики:** быстрый путь (infobox) 12/13 = 92% → **13/13 = 100%**; покрытие по всем
вопросам 13/31 = 42% → **14/31 = 45%**; false-fast **0% → 0%**; медиана латентности
поиска 847 → 786 мс (в пределах шума). ib14 «кто режиссёр фильма Сталкер» ушёл с
~50 с декода на инфобокс: «Андрей Тарковский» из «Сталкер (фильм)». Оба замера — на
S23, одним и тем же `run_probe.sh`, билды hexagon-25 и hexagon-27.
**Коммиты:** f67d696, 519ff74.
**Дальше:** скорер теряет ib02 «кто мэр Москвы» (в diag.log вопрос есть, в таблице
строки нет — RAG отвечает дольше, чем `sleep 3` в раннере). Из-за этого знаменатель
31, а не 32. Починить паузу/маркер в `run_probe.sh` — задача без телефона.

## 2026-08-26 11:39–11:50 (день)
**Сделано:** быстрый путь перестал упираться в первую же догадку — кандидаты берутся из
трёх источников (строгий exact-title, `EntityTitleProbe`, братья по префиксу), и перебор
идёт, пока не найдётся статья с запрошенным полем карточки.
**Метрики:** быстрый путь (infobox) 11/14 = 79% → **13/14 = 93%** (ib11 «Толстой, Лев
Николаевич», ib13 «Война и мир»); покрытие по всем 32: 38% → **42%**; false-fast **0% → 0%**.
Эти два вопроса ушли с ~50 с декода на ~0.5 с. recall@k не менялся (правка его не трогает):
53–56% @1, 91–97% @3 — разброс между двумя прогонами одного билда, то есть шум.
**Коммиты:** 7a9375f.
**Дальше:** ib14 «Сталкер (фильм)» всё ещё мимо — `findByTitlePrefix('Сталкер')` не отдаёт
скобочных братьев, хотя BM25 их видит. Проверить, что именно возвращает title-префикс.

## 2026-08-26 02:51–02:59 (ночь)
**Сделано:** попытка поднять recall@1 — убрать перезапись порядка title-boost'ом; откачена через 8 минут.
**Метрики:** recall@1 53% → 53% (без изменений, правка не дала эффекта).
**Коммиты:** 829b7ff, откат 41e92a3.
**Дальше:** порядок от `EntityTitleProbe.probe()` теряется между ним и логом `Top hits`. Любая правка скоринга бессмысленна, пока перестановка не найдена. План: залогировать список после `distinctBy { it.path }` и после сортировки в `gatherSortedHits`, прогнать mh04, сравнить.

## 2026-08-25 23:16–23:41 (ночь)
**Сделано:** поиск сущности по индексу заголовков, а не только через BM25.
**Метрики:** recall@3 62% → **97%**, recall@1 44% → 53%, MRR 0.535 → 0.729, латентность поиска 824 → **644 мс**. Потерянных статей 11 → **1**.
**Коммиты:** 4880f88, замер 578d7a1.
**Дальше:** упереться в recall@1 (см. запись выше).

## 2026-08-25 21:20–21:29 (день)
**Сделано:** retrieval-only режим probe + раннер и скорер; впервые снят базовый замер на устройстве.
**Метрики:** recall@1 **44%**, recall@3 **62%**, recall@20 **66%**, MRR 0.535, быстрый путь 79% (инфобокс) / 38% (все), false-fast-rate **0%**, латентность поиска 824 мс.
**Коммиты:** d1d57c6, 2182dd9.
**Дальше:** 11 из 32 статей не попадают в топ-20 — BM25 цепляется за словоформы («Казань» проигрывает «Архитектуре Казани»).

---

## Известные проблемы

- **`build.yml` красный с 25.08.** `CMakeLists.txt` прибит к llama.cpp `b9296`, где ещё нет MTP-API (`load_mtp`, `llama_model_n_layer_nextn`, `ctx_other`, новая сигнатура `llama_sampler_init_penalties`). `hexagon-app.yml` клонирует master и собирается нормально — рабочие APK берутся оттуда. Починка: свести оба воркфлоу к одному пину llama.cpp.
- **recall@1 застрял на 53%** — см. запись за 02:51. Важно: два прогона одного и того же
  билда 26.08 дали 53% и 56% @1 (и 97% / 91% @3), отдельные вопросы прыгали на 3 ранга.
  То есть разницу меньше ~5 п.п. по recall на этом наборе мерять бессмысленно — нужен
  либо усреднённый прогон, либо детерминированный реранк.
- **pr03 («кто открыл пенициллин») неисправим** в этом снапшоте: «Пенициллин» — редирект на «Бензилпенициллин». Эталон под наш вывод не подгонять (см. предупреждение о циклическом бенчмарке в README.md).
